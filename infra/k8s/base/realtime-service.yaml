apiVersion: v1
kind: Service
metadata:
  name: realtime
  annotations:
    # Sticky sessions for WebSocket upgrade affinity.
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  selector: { app: realtime }
  sessionAffinity: ClientIP
  ports:
    - port: 80
      targetPort: 4001
