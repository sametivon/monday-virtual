/**
 * In-scene typeface (S6): Inter Medium, self-hosted woff (troika reads
 * woff v1/ttf, NOT woff2). Every drei <Text> passes this so 3D type matches
 * the product's UI instead of the engine's fallback font.
 */
export const SCENE_FONT = '/fonts/inter-medium.woff';
